services:
  - type: web
    name: excelbase
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      # Geçici v6 migration modu: gerçek kullanıcı auth eklenene kadar mevcut
      # Streamlit benzeri tek-kullanıcı/public davranışı korur.
      - key: GATEVISA_ALLOW_DEV_NO_AUTH
        value: "1"
