# PP-OCRv6 synthetic image protocol

RUN with the optional PaddleOCR engine. Evidence is in `ppocrv6-synthetic.json`; all persisted source images and the image-only PDF under `sources/` are synthetic.
